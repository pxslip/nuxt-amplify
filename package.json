{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "serve": "node .output/server/index.mjs",
    "start": "node .output/server/index.mjs",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "^3.1.0"
  },
  "dependencies": {
    "@aws-amplify/ui-vue": "^3.1.6",
    "aws-amplify": "^5.0.12",
    "next": "^13.1.6"
  }
}
